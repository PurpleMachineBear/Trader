# iter_073 Executive Report

## Decision

Keep `platform5 pre1 hold3` as the cloud event-aware shadow leader. Do not promote it beyond shadow status.

## Why

- `2025 platform5 pre1 hold3`: `97.990%` return, `10.0%` drawdown
- `2025 platform7 pre1 hold3`: `58.295%`, `18.1%` drawdown
- `2026 YTD` for both rows: `5.078%`, `3.5%` drawdown, only `2` trades

## Final View

- The narrower `platform5` basket is a real improvement inside the meaningful `2025` window.
- The `2026 YTD` sample is too sparse for promotion.
- This is now the strongest cloud-only large-cap event branch, but still not a frozen deployment candidate.

## Recommendation

- Keep the frozen paper set unchanged.
- Carry `platform5 pre1 hold3` forward only as a cloud shadow branch.
- Prioritize earlier-window validation over more micro-tuning.
