# iter_079 Executive Report

## Decision

Keep `platform5 pre1 hold3` as the canonical cloud control. Demote `enterprise4` from promotion consideration and keep `software3` only as a hostile-window alias.

## Why

- `2024 platform5`: `10.449%` return, `19.5%` drawdown
- `2024 enterprise4`: `10.306%`, `22.6%` drawdown
- `2024 software3`: `22.154%`, `22.5%` drawdown
- `2026 YTD` for all three baskets: identical `5.078%` return on `2` trades

## Final View

- `software3` is the best hostile-window variant.
- `enterprise4` is not the right promotion path because it does not improve hostile `2024`.
- The current sparse window provides no basket-specific evidence, so it cannot settle the promotion question.

## Recommendation

- Keep the frozen paper set unchanged.
- Keep `platform5 pre1 hold3` as the cloud control.
- Run one explicit `2025` basket round next to complete the window map.
