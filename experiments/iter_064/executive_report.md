# iter_064 Executive Report

## Decision

The first next-gen large-cap selector pass was successful. `growth4 BSL` materially improved once ranking and qualification emphasized premarket quality and key-level proximity.

## Winner

- `growth4 BSL next-gen pool2`: `24.033%` return, `3.1%` drawdown
- `growth4 BSL next-gen pool1`: `23.775%` return, `2.4%` drawdown

## What Changed

- The old `growth4 BSL pool2` control improved from `13.685% / 5.6% DD` to roughly `24% / 3% DD`.
- `quality6` became directionally viable under the same selector logic.
- `hardware7` still failed under `BSL`, reinforcing the view that it wants a different family.

## Recommendation

- Validate the new `growth4` selector by subwindow before promotion.
- Do not spend more budget trying to rescue `hardware7` under `BSL`.
