# iter_080 Executive Report

## Decision

Close the first cloud basket-discovery campaign. Keep `platform5 pre1 hold3` as the canonical cloud control, `enterprise4 pre1 hold3` as the positive-window alias, and `software3 pre1 hold3` as the hostile-window alias.

## Why

- `2025 platform5`: `97.990%` return, `10.0%` drawdown
- `2025 enterprise4`: `105.047%`, `9.9%` drawdown
- `2025 software3`: `91.320%`, `9.1%` drawdown
- together with prior rounds:
  - `2024 software3` was best
  - `2026 YTD` was non-discriminating

## Final View

- The cloud lane now has a complete basket map, but not a single promoted winner.
- `enterprise4` is the best positive-window refinement.
- `software3` is the best hostile-window refinement.
- `platform5` remains the canonical control because it is the least fragile cross-window reference.

## Recommendation

- Keep the frozen paper set unchanged.
- Stop near-term cloud basket permutation work.
- If this lane continues, shift effort toward richer event metadata and state detection instead of more basket tuning.
