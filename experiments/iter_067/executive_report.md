# iter_067 Executive Report

## Decision

Promote `hardware7 failed_breakdown next-gen pool2` only to the `shadow` lane, not to the frozen paper set.

## Why

- It improved the old hardware7 branch in `2024`, `2025`, and `2026 YTD`.
- It became the best tested large-cap alternative shadow branch.
- It still did not beat the old `growth4 failed_breakdown` hostile-window reference in `2024`.

## Final View

- `hardware7 failed_breakdown next-gen pool2` is real.
- It is stronger than the old hardware7 branch.
- It is still not all-weather.

## Recommendation

- Freeze the large-cap paper set unchanged.
- Carry the new hardware7 branch forward only as a shadow/regime candidate.
