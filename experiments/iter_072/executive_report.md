# iter_072 Executive Report

## Decision

Promote `platform5 pre1 hold3` only to the cloud event-aware lead. Do not promote it beyond shadow status yet.

## Why

- `platform5 pre1 hold3`: `100.059%` return, `10.1%` drawdown
- `platform7 pre1 hold3`: `59.951%`, `18.1%` drawdown
- `platform6 no NFLX`: `76.942%`, `16.8%` drawdown
- `platform6 no ADBE`: `80.838%`, `19.5%` drawdown
- `max_names` changes did not matter

## Final View

- The platform event branch is highly sensitive to basket quality.
- The strongest improvement came from removing `NFLX` and `ADBE`.
- This is a real lead, but still an in-sample refinement until it passes window validation.

## Recommendation

- Keep the frozen paper set unchanged.
- Validate `platform5 pre1 hold3` by window before giving it any stronger roadmap status.
